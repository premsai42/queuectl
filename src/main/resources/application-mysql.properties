# MySQL connection ? change username/password as needed
spring.datasource.url=jdbc:mysql://localhost:3306/queuectl?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=your_password_here
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# disable flyway
spring.flyway.enabled=false

queuectl.data.dir=.queuectl
spring.shell.history.name=.queuectl-history
